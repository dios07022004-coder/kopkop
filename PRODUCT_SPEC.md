# Спецификация продукта: «Деньги под контролем»
## Финансовый трекер бюджета + калькуляторы решений

**Версия документа:** 1.1  
**Дата:** 12.06.2026  
**Формат продукта:** Цифровой (Google Sheets / Excel + HTML-инструкция + продающий сайт + калькуляторы на /app)  
**Цена:** 399 ₽  
**Язык:** Русский  

---

## 1. Суть продукта

### 1.1. Одно предложение
Инструмент, который за 10 минут отвечает на 4 вопроса: сколько денег остаётся, сколько можно тратить, куда распределить траты, и стоит ли покупать желаемую вещь прямо сейчас.

### 1.2. Что покупает клиент
Не «финансовое образование», а **готовое решение** — таблица с формулами + 2 калькулятора + инструкция.

### 1.3. Четыре ключевых вопроса продукта

| # | Вопрос пользователя | Где ответ |
|---|---------------------|-----------|
| 1 | Сколько денег реально остаётся каждый месяц? | Дашборд, калькулятор «Куда тратить» |
| 2 | Сколько можно откладывать без стресса? | Лист «Резерв», «Цели» |
| 3 | Могу ли купить желаемую вещь сейчас? | Калькулятор «Купить или нет» |
| 4 | Если не могу — когда смогу? | Расчёт «Отложить на N месяцев» |

### 1.4. Целевая аудитория

| Сегмент | Боль | Решение в продукте |
|---------|------|-------------------|
| **Студент** | Мало денег, непонятно сколько можно тратить | Простой лимит трат |
| **Фрилансер** | Нерегулярный доход | Резерв на просадки, доходы по клиентам |
| **Семья** | Много категорий расходов | Общий бюджет, дети, коммуналка |

### 1.5. Позиционирование
- **Не:** инвестиции, консалтинг, сложный финтех
- **Да:** личный инструмент, мгновенный ответ, спокойствие при покупках

---

## 2. Состав продукта

### 2.1. Цифровой комплект (399 ₽)

| Компонент | Формат | Описание |
|-----------|--------|----------|
| Таблица-бюджет | Google Sheets + Excel (.xlsx) | 8 вкладок с формулами |
| Калькулятор «Мой план денег» | На сайте `/app` | План сверху + галочки «Коплю» / «Хочу купить» |
| Калькулятор «Купить или нет» | В таблице + `/app` (блок «Проверить покупку») | Решение о покупке |
| HTML-инструкция | private/downloads/instruction.html | Старт за 10 минут (можно сохранить как PDF) |
| 3 сценария | В одном файле | Базовый, фрилансер, семья |

### 2.2. Продающий сайт

| Страница | URL | Назначение |
|----------|-----|------------|
| Лендинг | `/` | Продажа, один CTA → checkout |
| Checkout | `/checkout` | Форма + оплата YooKassa (399 ₽) |
| Success | `/success?order_id=...` | «Проверьте почту — логин и пароль» |
| Login | `/login` | Вход после оплаты |
| App | `/app` | «Мой план денег» — план сверху, галочки, цифры (protected) |
| Account | `/account` | Файлы и ссылки (protected) |
| FAQ | `/faq` | Ответы на вопросы |
| Оферта | `/terms` | Юридический документ |
| Privacy | `/privacy` | Политика конфиденциальности |

> `/demo` перенаправляет на `/checkout` — публичного доступа к калькуляторам нет.

### 2.3. Технический стек сайта

- Next.js 15 (App Router)
- TypeScript
- Tailwind CSS + shadcn/ui
- react-hook-form + zod
- YooKassa API (оплата)
- Supabase Auth + `@supabase/ssr`
- Resend (email с логином/паролем)
- Заказы dev: `data/orders.json`

---

## 3. Единое финансовое ядро

Все калькуляторы (сайт, Excel, API) используют **один** модуль `src/lib/finance/` с функцией `computeBudgetCore()` и `decidePurchase()`.

### 3.1. Глоссарий сущностей

| ID | Русское имя в UI | Горизонт | Описание |
|----|------------------|----------|----------|
| `currentBalance` | На счёте сейчас | snapshot | Текущий остаток |
| `incomeMonthly` | Доход за месяц | month | Ожидаемый доход до конца периода |
| `mandatoryMonthly` | Обязательные + долги | month | Аренда, кредиты, коммуналка (сумма) |
| `plannedVariableMonthly` | Плановые переменные траты | month | Еда, транспорт — уже заложено в бюджет |
| `savingsMonthly` | Копилка / цели | month | Ежемесячное отложение |
| `minimumBalance` | Минимум на счёте | snapshot | Операционный пол — ниже нельзя опускаться |
| `reserveTarget` | Цель подушки (опц.) | goal | Для progress bar, не блокирует траты |

**Важно:** `minimumBalance` ≠ `reserveTarget`. Минимум на счёте защищает от импульсивных трат; цель подушки — долгосрочная накопительная цель (лист «Резерв» в Excel).

### 3.2. Входные данные (единый контракт `BudgetInput`)

| Поле | Описание | Пример |
|------|----------|--------|
| `incomeMonthly` | Доход за месяц | 80 000 ₽ |
| `currentBalance` | На счёте сейчас | 45 000 ₽ |
| `mandatoryMonthly` | Обязательные траты (итого) | 40 000 ₽ |
| `mandatoryExpenses[]` | Строки обязательных трат | Аренда 25 000, Кредиты 10 000, Коммуналка 5 000 |
| `plannedVariableMonthly` | Плановые переменные | 15 000 ₽ |
| `savingsMonthly` | Копилка / цели в месяц | 10 000 ₽ |
| `minimumBalance` | Минимум на счёте | 30 000 ₽ |
| `categories[]` | Категории распределения | Продукты 35%, Транспорт 15%... |

Валидация: `budgetInputSchema` (zod) + `parseBudgetInput()` — все поля ≥ 0, NaN → 0.

### 3.3. Формулы ядра

```
remainingAfterMandatory = incomeMonthly − mandatoryMonthly

freeBudgetMonthly = remainingAfterMandatory − plannedVariableMonthly − savingsMonthly
  (может быть < 0 — сигнал stress, не обрезается)

totalResources = currentBalance + incomeMonthly

availableForLife = MAX(0, totalResources − mandatoryMonthly − savingsMonthly − minimumBalance)

upcomingMandatoryFromBalance = MIN(currentBalance, mandatoryMonthly)

availableForPurchaseNow = MAX(0, currentBalance − minimumBalance − upcomingMandatoryFromBalance)

reserveProgress = reserveTarget > 0 ? MIN(100, currentBalance / reserveTarget × 100) : 100
```

### 3.4. Статус бюджета (`safetyStatus`)

Итог = **худший** из трёх показателей (conservative):

| Показатель | healthy | caution | danger |
|------------|---------|---------|--------|
| `freeBudgetMonthly` | ≥ 5 000 ₽ | 0..4 999 ₽ | < 0 |
| `reserveProgress` | ≥ 80% | 50..79% | < 50% |
| `availableForLife` | > 0 | < 10 000 ₽ | ≤ 0 |

Пороги в `src/lib/finance/constants.ts`.

---

## 4. Калькулятор «Куда тратить»

### 4.1. Назначение
Показать **на жизнь в месяц** (`availableForLife`) и **распределить** по категориям. Базовые цифры общие с калькулятором покупки.

### 4.2. Распределение по категориям

База: `availableForLife` (не `freeBudgetMonthly`).

```
amount[i] = ROUND(availableForLife × percent[i] / totalPercent)
остаток от округления → категория «Прочее» (фиксированное правило)
```

**Категории по умолчанию:**

| Категория | % |
|-----------|---|
| Продукты | 35% |
| Транспорт | 15% |
| Здоровье / аптека | 10% |
| Развлечения | 10% |
| Прочее | 30% |

### 4.3. Статус распределения

Использует единый `safetyStatus` из ядра + порог «осторожно тратить сейчас» при `availableForLife < 10 000` или `safeToSpendNow < 5 000`.

---

## 5. Калькулятор «Купить или нет»

### 5.1. Назначение
Принять решение о покупке на основе **единого ядра** + параметров покупки.

### 5.2. Дополнительные входные данные (`PurchaseInput`)

| Поле | Описание | Шкала |
|------|----------|-------|
| `price` | Цена покупки | ₽ |
| `urgency` | Срочность | 1–5 (5 = очень срочно) |
| `usefulness` | Полезность / нужность | 1–5 (5 = очень нужно) |

Валидация: `purchaseInputSchema` + `parsePurchaseInput()`.

### 5.3. Промежуточные расчёты

```
missingAmount = MAX(0, price − availableForPurchaseNow)

monthsToPurchase = missing > 0 AND freeBudgetMonthly > 0 ? CEIL(missing / freeBudgetMonthly) : null

reserveAfterPurchase = currentBalance − price
```

### 5.4. Дерево решений (приоритет сверху вниз)

```
1. IF (можно оплатить с баланса) AND (reserveAfter < minimumBalance)
   → build_reserve (Сначала резерв)

2. ELSE IF missing > 0 AND freeBudgetMonthly ≤ 0
   → defer (Сначала стабилизируйте бюджет)

3. ELSE IF missing > 0 AND freeBudgetMonthly > 0
   → defer (Отложить на N месяцев)

4. ELSE IF canAffordSafely AND freeBudgetMonthly < 0
   → defer (не «подождать 7 дней» при отрицательном потоке)

5. ELSE IF canAffordSafely AND urgency ≤ 2 AND usefulness ≤ 2
   → wait (Подождать 7 дней)

6. ELSE IF canAffordSafely AND freeBudgetMonthly ≥ 0
   → buy_now (Купить сейчас)

7. ELSE → defer
```

### 5.5. Итоговые статусы

| Статус | Код | Когда |
|--------|-----|-------|
| Купить сейчас | `buy_now` | Деньги есть, резерв не пострадает, поток ≥ 0 |
| Подождать 7 дней | `wait` | Можно купить, но не срочно и не критично |
| Отложить | `defer` | Не хватает или отрицательный месячный поток |
| Сначала резерв | `build_reserve` | На балансе хватает, но после покупки ниже minimumBalance |

### 5.6. Психологический слой
Поля **срочность** и **полезность** (порог ≤ 2) защищают от импульсивных покупок. Тексты в `src/lib/finance/copy.ts`: что / почему / следующий шаг.

---

## 6. Структура Excel / Google Sheets таблицы

### 6.1. Вкладки (7 листов)

| # | Лист | Назначение | Ключевые поля |
|---|------|------------|---------------|
| 1 | **Старт** | Инструкция, легенда цветов | Чек-лист из 6 шагов |
| 2 | **Доходы** | Учёт доходов | Дата, источник, категория, сумма |
| 3 | **Расходы** | Учёт расходов | Дата, категория, тип (обязательный/переменный), сумма |
| 4 | **Распределение** | Калькулятор «Куда тратить» | Счёт, доход, обязательные, минимум, категории |
| 5 | **Резерв** | Подушка безопасности | Текущий резерв, цель, прогресс % |
| 6 | **Решение о покупке** | Калькулятор покупки | Цена, срочность, полезность, статус |
| 7 | **Цели** | Долгосрочные накопления | Цель, сумма, срок, прогресс % |
| 8 | **Дашборд** | Сводка | Доход, расходы, свободный бюджет, статус |

### 6.2. Связи между листами

```
Доходы ──────┐
             ├──→ Дашборд ←── Резерв
Расходы ─────┤
             │
Распределение┘──→ «Можно тратить без риска» на Дашборде
```

### 6.3. Три сценария в одном файле

| Версия | Как использовать |
|--------|------------------|
| **Базовая** | Стандартное заполнение всех листов |
| **Фрилансер** | Лист «Доходы» — категория «Фриланс/Клиент»; «Резерв» — просадка на N месяцев |
| **Семья** | Лист «Расходы» — подкategории: дети, продукты, коммуналка |

---

## 7. Оформление и UX

### 7.1. Дизайн-концепция
- **Стиль:** Спокойный финтех, минимализм
- **Настроение:** Доверие, простота, «воздух»
- **Не использовать:** Сложные графики, финансовый жаргон, тёмные агрессивные цвета

### 7.2. Цветовая система

| Token | HSL | Применение |
|-------|-----|------------|
| Background | `40 33% 98%` | Тёплый белый фон |
| Primary | `160 55% 38%` | Кнопки, акценты (мягкий зелёный) |
| Success | `160 55% 38%` | «Можно покупать», бюджет OK |
| Warning | `38 92% 50%` | «Подумать», осторожно |
| Danger | `0 65% 52%` | «Нельзя», опасно |
| Muted | `40 20% 94%` | Фоны секций, вторичный текст |
| Border | `40 15% 90%` | Мягкие границы карточек |

### 7.3. Типографика
- **Шрифт:** Inter (latin + cyrillic)
- **Hero заголовок:** 1.75rem (mobile) → 3.5rem (desktop), bold
- **Body:** 16px base, line-height relaxed
- **Цифры в калькуляторах:** xl–2xl, bold — крупные и читаемые

### 7.4. Компоненты UI

| Компонент | Стиль |
|-----------|-------|
| Карточки | `rounded-2xl`, мягкая тень, border 60% opacity |
| Кнопки CTA | `rounded-xl`, h-12, primary green |
| Поля ввода | h-12, `inputMode="numeric"`, крупный текст |
| Badge статуса | Pill-форма, цвет по статусу |
| Progress bar | h-2–3, цвет по статусу бюджета |

### 7.5. Структура лендинга

| # | Секция | ID | Цель |
|---|--------|-----|------|
| 1 | Hero | — | Захват за 10 сек: заголовок + 2 CTA |
| 2 | Как работает (3 шага) | `#how` | Простое объяснение |
| 3 | Знакомо? (боли) | `#problems` | Эмпатия |
| 4 | Что получаете | `#inside` | Состав продукта |
| 5 | Три шага | `#how-it-works` | Механика |
| 6 | Для кого | `#for-whom` | Сегменты |
| 7 | Примеры решений | — | Social proof логики |
| 8 | Три версии | — | Ценность bundle |
| 9 | Отзывы | — | Social proof |
| 10 | Цена | `#pricing` | Конверсия |
| 11 | FAQ | — | Снятие возражений |
| 12 | CTA финальный | — | Последний push |

### 7.6. Копирайт (ключевые тексты)

| Элемент | Текст |
|---------|-------|
| Название | «Деньги под контролем» |
| Hero | «Перестаньте гадать с деньгами» |
| Subtitle | «Введите доход и траты — получите готовый план: сколько тратить, куда, и можно ли купить то, что хочется.» |
| CTA Demo | «Попробовать бесплатно» |
| CTA Buy | «Получить за 399 ₽» |

### 7.7. Mobile-first UX

| Принцип | Реализация |
|---------|------------|
| Результат сверху | Панель «Ваш план» — тратить, копить, покупать |
| Крупные touch-targets | Кнопки h-12, поля h-11–h-12 |
| Меню | Slide-in drawer справа |
| Опции | Галочки «Коплю» / «Хочу купить» / «Плановые траты» вместо вкладок |
| Safe area | `env(safe-area-inset-bottom)` для iPhone |

---

## 8. Пользовательские сценарии (User Flows)

### 8.1. Бесплатное знакомство
```
Лендинг → «Попробовать бесплатно» → /demo → Вкладка «Траты» или «Покупка»
→ Ввод цифр → Мгновенный результат → CTA «Получить полную таблицу»
```

### 8.2. Покупка
```
Лендинг → «Получить за 590 ₽» → /checkout → Email + имя + согласие
→ YooKassa оплата → /success → Ссылки: Google Sheets, Excel, PDF
→ Email с ссылками (TODO)
```

### 8.3. Работа с таблицей (после покупки)
```
Старт (10 мин инструкция) → Доходы → Расходы → Распределение
→ Резерв → [Покупка?] → Решение о покупке → Дашборд (ежемесячно)
```

---

## 9. Пример расчёта (дефолтные значения)

### Входные данные
- Доход: 80 000 ₽
- Обязательные: 40 000 ₽ (аренда 25 000 + кредиты 10 000 + коммуналка 5 000)
- Плановые переменные: 15 000 ₽, копилка: 10 000 ₽
- На счёте: 45 000 ₽, минимум на счёте: 30 000 ₽
- Покупка: ноутбук 35 000 ₽, срочность 3, полезность 4

### Расчёт «Куда тратить»
```
Свободный поток = 80 000 − 40 000 − 15 000 − 10 000 = 15 000 ₽
Всего ресурсов = 45 000 + 80 000 = 125 000 ₽
На жизнь = MAX(0, 125 000 − 40 000 − 10 000 − 30 000) = 45 000 ₽
Продукты (35%) = 15 750 ₽, Транспорт (15%) = 6 750 ₽
```

### Расчёт «Купить или нет»
```
Обязательные с баланса = MIN(45 000, 40 000) = 40 000 ₽
Доступно на покупку = MAX(0, 45 000 − 30 000 − 40 000) = 0 ₽
Не хватает = 35 000 ₽
После покупки на счёте = 45 000 − 35 000 = 10 000 ₽ (< минимум 30 000)
→ Результат: «Сначала резерв» (build_reserve)
```

Объяснение: на балансе формально хватает на ноутбук, но обязательные платежи «съедают» счёт, а после покупки подушка упадёт ниже минимума.

---

## 10. Монетизация и доставка

| Параметр | Значение |
|----------|----------|
| Цена | 399 ₽ |
| Платёжная система | YooKassa |
| Доставка | Мгновенная (ссылки на сайте + email) |
| Форматы | Google Sheets (copy link), Excel (.xlsx), HTML-инструкция |

### Альтернативные ценовые tier'ы (задумка)
| Tier | Цена | Состав |
|------|------|--------|
| Базовая | 590 ₽ | Таблица + инструкция |
| Расширенная | 790 ₽ | + PDF + 3 сценария |
| Bundle | 990 ₽ | + семья + фрилансер + студент |

*Текущая реализация: один tier 590 ₽ со всем включённым.*

---

## 11. Статус реализации

### ✅ Реализовано
- Продающий сайт (7 страниц)
- Единое финансовое ядро `src/lib/finance/` (core, allocation, purchase-decision)
- Демо с общим вводом «Базовые цифры» + блок «Разбор расчёта»
- 22 unit-теста (Vitest) на формулы и дерево решений
- Excel-шаблон синхронизирован с ядром (`scripts/finance-spec.mjs`)
- HTML-инструкция
- API YooKassa (create + webhook)
- Mobile-first дизайн
- Checkout форма с валидацией
- Zod-валидация входов (`budgetInputSchema`, `purchaseInputSchema`)

### ⏳ Не реализовано (TODO для продакшена)
- Реальные ключи YooKassa
- Google Sheets шаблон (загрузить и указать URL)
- Email-рассылка после оплаты (Resend)
- База данных заказов (Supabase вместо JSON-файла)
- PDF-экспорт инструкции
- PWA (установка на телефон)
- Яндекс.Метрика / аналитика
- Реальный support-email

---

## 12. Ограничения и дисклеймеры

- Продукт **не является** финансовой, инвестиционной или налоговой консультацией
- Решения носят **рекомендательный** характер
- Пользователь принимает решения **самостоятельно**
- Формулы основаны на введённых пользователем данных — garbage in, garbage out

---

## 13. Конкурентные преимущества

| vs Конкуренты | Наш продукт |
|---------------|-------------|
| Сложные фин-приложения | Простые поля, готовый ответ |
| Обычные таблицы | Встроенные калькуляторы решений |
| «Обучение финансам» | Мгновенный ответ без теории |
| Дорогие подписки | Разовая оплата 590 ₽ |
| Только учёт | Учёт + решение о покупке |

---

## 14. KPI для анализа

| Метрика | Цель |
|---------|------|
| Конверсия лендинг → демо | > 15% |
| Конверсия демо → checkout | > 5% |
| Конверсия checkout → оплата | > 30% |
| Возвраты | < 5% |
| NPS | > 40 |

---

*Документ подготовлен для анализа продукта, UX, бизнес-логики и готовности к запуску.*
